/** Legacy Prisma helper — product images come from SMOOTHIE_MENU + scripts/seed.ts. */
console.log("update-product-images is retired. Re-run npm run db:seed after changing smoothie-menu.ts.");
process.exit(0);
